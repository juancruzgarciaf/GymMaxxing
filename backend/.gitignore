node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma
# Uploaded profile and training media stays local.
uploads/profiles
uploads/trainings

# Exercise media uploaded by the admin is shared with the team.
!uploads/exercises/
!uploads/exercises/**
!uploads/.gitkeep
